import React from "react";

interface props {
  params: any;
}

export default function Unitdetails({ params }: props) {
  console.log("params", params);
  return <>To be used as the new units page once apis are ready</>;
}
