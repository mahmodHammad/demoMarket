export interface MultilineDataset {
	label: string;
	data: [];
	borderColor: string;
	backgroundColor: string;
}

export interface DoughnutDataset {
	name: string;
	value: string | number;
	color: string;
}